{
  "name": "industry-heatmap",
  "version": "1.1.0",
  "description": "A股行业新高/新低热力图 - macOS Desktop App",
  "main": "dist/electron/main.js",
  "author": "",
  "license": "MIT",
  "scripts": {
    "dev": "tsc -p tsconfig.electron.json && electron .",
    "build": "tsc -p tsconfig.electron.json && electron-builder",
    "build:mac": "tsc -p tsconfig.electron.json && electron-builder --mac --arm64",
    "postinstall": "tsc -p tsconfig.electron.json || true"
  },
  "dependencies": {
    "node-cron": "^4.2.1"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "electron": "^40.10.6",
    "electron-builder": "^26.15.3",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.0"
  }
}
